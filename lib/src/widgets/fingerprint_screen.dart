import 'package:flutter/material.dart';

class FinderPrintScreen extends StatelessWidget {
  const FinderPrintScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
